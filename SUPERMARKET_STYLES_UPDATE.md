# SuperMarket Store Select Screen - UI Polish Update

## Summary of Changes

### Color Palette
- **Background**: Changed from `#f9fafb` to `#f8fafc` (slightly cooler tone)
- **Active Filter Chips**: Changed from `#dbeafe` to `#3b82f6` (vibrant blue with white text)
- **Borders**: Enhanced from hairline to `1px` solid borders with better contrast
- **Shadows**: Increased shadow intensity for better depth perception

### Search Bar
- **Border**: Increased from `1px` to `1.5px` with darker color (`#cbd5e1`)
- **Padding**: Increased from `8px` to `10px` vertical for better touch target
- **Border Radius**: Increased from `12px` to `14px` for modern look
- **Shadow**: Added subtle shadow for depth

### Filter Button
- **Background**: Changed to `#f1f5f9` with border
- **Text Weight**: Increased to `600` (semi-bold)
- **Border**: Added `1px` border in `#cbd5e1`

### Filter Chips (Active State)
- **Background**: Bright blue `#3b82f6` instead of light blue
- **Text Color**: White `#ffffff` instead of blue
- **Shadow**: Added blue glow effect for active state
- **Border**: Removed in active state, enhanced in inactive

### Expand/Collapse Buttons
- **Background**: Changed to blue tint `#dbeafe`
- **Border**: Added `1px` border in `#93c5fd`
- **Text**: Darker blue `#1e40af` with bold weight

### Brand/Category Headers
- **Brand Header**: Added background `#f8fafc` with rounded top corners
- **Category Header**: Added background `#f8fafc` with border radius `10px`
- **Padding**: Increased for better spacing

### Cards
- **Shadow**: Increased opacity and radius for better elevation
- **Border**: Added `1px` solid border
- **Info Button**: Larger padding with blue background

### Typography
- **Letter Spacing**: Added `-0.2` to `-0.3` for headers (tighter, modern look)
- **Font Weights**: Increased across the board (`600` → `700`)
- **Line Heights**: Added to companyTitle for better readability

